let firstName = "usher";
let lastName = "kadio";
let fullName = `${ firstName } ${ lastName }`;
console.log ( fullName );

/*===========*/
let searchResults = 50;
let output = `${ searchResults > 0
                 ? `${ searchResults } results`
                 : "no search results" }`;
console.log ( output );
